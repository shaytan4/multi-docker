# multi-docker
Training project ( Fibonacci numbers ) . Learning multi-docker proj - nodejs frontend (react) , backend , nginx url routing + redis + postgres.

CI  - Use Travis

Push image to docker hub
